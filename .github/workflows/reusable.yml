name: Octo Organization CI

on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: C:\Data\Github_Actions\.github\workflows\adding_A_file.yml
        with:
          file_name: my_app
      - name: Run a one-line script
        run: echo Hello from Octo Organization
       
          

   